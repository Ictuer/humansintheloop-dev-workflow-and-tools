You are implementing the following application:

* Idea: @{{ idea_directory }}/*-idea.*
* Specification: @{{ idea_directory }}/*-spec.md
* Implementation tasks: @{{ idea_directory }}/*-plan.md

Your task:

{{ task_description }}

Follow the active TDD and plan-tracking skills:

Do not work on future tasks until the current one is complete.
If all tasks are complete, stop and report success.
{% if allow_push -%}
You may push the current branch to origin, never with --force and never another branch, when the task needs CI to run on a new commit before it can be completed. The caller still pushes after the task.
{% else -%}
Do not push to the remote repository - the caller handles pushing.
{% endif -%}
{% if extra_prompt %}

{{ extra_prompt }}
{% endif %}
{% if not interactive %}

IMPORTANT: if you don't have permission to perform an action, print an informative error message and exit
{%- endif %}

CRITICAL:

Immediately, before exiting output one of the following messages
- If successful: <SUCCESS>task implemented: COMMIT_SHA</SUCCESS>
- If failed: <FAILURE>Explanation of failure - if permission include those required permissions</FAILURE>
